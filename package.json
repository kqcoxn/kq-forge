{
  "name": "kq-forge",
  "version": "0.1.0",
  "description": "模块化 AI 编码代理 Harness",
  "type": "module",
  "bin": {
    "kq-forge": "./packages/cli/dist/index.js"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "pnpm -r run clean",
    "typecheck": "pnpm -r run typecheck",
    "prepare": "pnpm run build"
  },
  "dependencies": {
    "citty": "^0.1.6",
    "consola": "^3.4.0",
    "gray-matter": "^4.0.3",
    "yaml": "^2.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^25.8.0"
  }
}
